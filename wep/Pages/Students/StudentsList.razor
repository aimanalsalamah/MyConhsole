@page "/Students"
@inject NavigationManager _nav
<h3>العملاء</h3>
<button @onclick="GoToNewPage">اضافة زبون جديد</button>
<table class="table">
    <tr>
        <th>الاسم</th>
        <th>العمر</th>
        <th></th>
    </tr>
    @foreach (var item in this.Students)
   {
        <tr>
            <td>@item.Name</td>
            <td>@item.Age</td>
            <td>
                <button @onclick="()=> GoToDetailsPage(item.Id)">التفاصيل</button>
                <button @onclick="()=> GoToUpdatePage(item.Id)">تعديل</button>
                <button @onclick="()=> GoToDeletePage(item.Id)">حذف</button>
            </td>
        </tr>
   }
</table>

@code {
    public List<Tools.Models.Students> Students { get; set; } = new();
    protected override async Task OnInitializedAsync()
    {
        var client = new HttpClient();
        var response = await client.GetAsync("https://localhost:7110/Students");
        Students = await response.Content.ReadAsAsync<List<Tools.Models.Students>>();
    }
    void GoToNewPage()
    {
        _nav.NavigateTo("newStudents");
    }
    void GoToDetailsPage(int id)
    {
        _nav.NavigateTo("StudentsDetails/" + id);
    }
    void GoToDeletePage(int id)
    {
        _nav.NavigateTo("DeleteStudents/" + id);
    }
    void GoToUpdatePage(int id)
    {
        _nav.NavigateTo("UpdateStudents/" + id);
    }
}